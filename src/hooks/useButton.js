import { useState } from "react";

export function useButton() {
	// const handleClick = (e) => {
	// 	setTypeOfSearch(e.target.textContent);
	// };

	// return [typeOfSearch, handleClick];
	return null;
}
